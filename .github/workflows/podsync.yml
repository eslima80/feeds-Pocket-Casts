name: Run Podsync

on:
  schedule:
    - cron: '0 */1 * * *'  # Executa a cada 1 horas
  workflow_dispatch:       # Permite execução manual

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Criar pasta de saída
        run: mkdir -p data

      - name: Criar arquivo cookies.txt a partir do Secret
        run: |
          echo "${{ secrets.YOUTUBE_COOKIES }}" | awk '{print}' > .github/podsync/cookies.txt

      - name: Rodar Podsync com config.toml e cookies
        run: |
          docker run --rm \
            -v ${{ github.workspace }}/.github/podsync/config.toml:/app/config.toml \
            -v ${{ github.workspace }}/.github/podsync/cookies.txt:/app/cookies.txt \
            -v ${{ github.workspace }}/data:/app/data \
            ghcr.io/mxpv/podsync:latest \
            --cookies /app/cookies.txt

      - name: Copiar feeds para pasta docs
        run: |
          mkdir -p docs
          cp -r data/* docs/

      - name: Commit e push dos feeds
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add docs/
          git diff --cached --quiet || git commit -m "Atualiza feeds do Podsync"
          git pull --rebase || true
          git push

